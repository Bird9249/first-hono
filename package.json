{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "generate": "drizzle-kit generate:sqlite --schema=./src/**/repository/schema.ts --out=./drizzle",
    "migrate": "bun run ./src/connections/database/migration.ts"
  },
  "dependencies": {
    "@libsql/client": "^0.4.3",
    "drizzle-orm": "^0.29.3",
    "hono": "^3.12.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.20.14"
  }
}
